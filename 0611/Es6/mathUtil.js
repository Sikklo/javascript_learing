export const PI = 3.14159;

export function getCircumreference(radius) {
  return PI * 2 * radius;
}

export function getArea(radius) {
  return PI * raius * radius;
}

export function getVoluem(params) {
  return (4 / 3) * PI * radius * radius * radius;
}
